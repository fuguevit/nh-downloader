#!/usr/bin/env php

<?php

require 'vendor/autoload.php';

use Fuguevit\NHDownloader\Command;
use Symfony\Component\Console\Application;

$application = new Application();

// register commands
$application->add(new Command);

$application->run();
